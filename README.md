# FLX
A lightweight application launcher and local bin, configured with Lua, for launching applications, scripts, and commands through user-defined aliases.

---

#ZShell/Bash - actually works with Dash I think

#Why subshell/setsid/nohup - due to the issues I had with Noctalia tying it to my ptty
#- not sure if optimal, but systems will have nohup and setsid anyways

#Why Lua and not bash only

#How to use it

#Lua config entries - apps being the only necessary one; editor is optional

#built for AppImages, and as a local bin during testing of scripts, then built for anything, like any commands, etc. with no need for millions of aliases in .rc

#also show examples of how ppl can use it to create home vars etc. in the config
#- tilde expansion works, but variable names don't.



